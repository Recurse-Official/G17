"use client";
import React from "react";
import CardPopularProducts from "../(components)/DashboardComp/CardPopularProducts";

interface Props {}

function Page(props: Props) {
  const {} = props;

  return (
    <div>
      <CardPopularProducts />
    </div>
  );
}

export default Page;
